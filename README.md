# dn-react-uistring library

`
npm i dn-react-uistring || yarn add dn-react-uistring
`

**dn-react-uistring library - is a library which contains useful component for display uistring.xml strings info.**
